/**
 * Outbound weather adapters — {@code OpenMeteoClientAdapter} implementing
 * {@code WeatherClientPort} (Adapter pattern; US009). The external API shape is
 * hidden behind the port so it can change without rippling inward (Protected
 * Variations; docs/architecture.md §6). Empty at US007 scaffolding stage by design.
 */
package com.nortadas.infrastructure.weather;
