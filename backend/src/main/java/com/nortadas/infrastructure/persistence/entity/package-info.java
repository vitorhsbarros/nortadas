/**
 * JPA data models ({@code @Entity} classes: BeachEntity, RegionEntity,
 * WeatherReadingEntity) shaped for Hibernate. ORM annotations are confined to
 * this package — the domain layer never carries them (docs/architecture.md §3, §8).
 * Lombok is welcome here. Empty at US007 scaffolding stage by design.
 */
package com.nortadas.infrastructure.persistence.entity;
