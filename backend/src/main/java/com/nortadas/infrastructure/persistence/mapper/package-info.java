/**
 * Explicit domain ⇄ JPA-entity mappers (Pure Fabrication) keeping the domain and
 * data models separate — never shared inheritance, never one class serving two
 * layers (docs/architecture.md §3). Empty at US007 scaffolding stage by design.
 */
package com.nortadas.infrastructure.persistence.mapper;
