/**
 * Spring Data JPA repositories plus the adapters implementing the
 * {@code com.nortadas.application.port} interfaces (Repository + Adapter
 * patterns). Nothing outside {@code infrastructure} references Spring Data types
 * directly (docs/architecture.md §8). Empty at US007 scaffolding stage by design.
 */
package com.nortadas.infrastructure.persistence.repository;
