/**
 * Driving adapters triggered by time — {@code WeatherDataScheduler} (US009)
 * invokes application use cases on a schedule; it contains no business logic
 * itself (docs/architecture.md §2). Empty at US007 scaffolding stage by design.
 */
package com.nortadas.scheduler;
