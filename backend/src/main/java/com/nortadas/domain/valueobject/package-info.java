/**
 * Domain value objects — small immutable types that validate themselves at
 * construction (BeachId, RegionId, Name, Latitude, Longitude, WindSpeed,
 * WindDirection, ...). Equality is value-based. Pure Java only, like the rest of
 * the domain layer (docs/architecture.md §3.1).
 */
package com.nortadas.domain.valueobject;
