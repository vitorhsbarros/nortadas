/**
 * Outbound ports — interfaces only, e.g. {@code BeachRepositoryPort},
 * {@code WeatherClientPort}. Implemented by {@code infrastructure} adapters and
 * wired to use cases in {@code config} (Indirection / Protected Variations;
 * docs/architecture.md §1, §6). Empty at US007 scaffolding stage by design.
 */
package com.nortadas.application.port;
