/**
 * Inbound HTTP adapters — Spring {@code @RestController}s (BeachController,
 * RegionController; US011/US012). GRASP controllers only: translate HTTP ⇄ a use
 * case call, no business logic (docs/architecture.md §5, §6). Empty at US007
 * scaffolding stage by design.
 */
package com.nortadas.web.controller;
