/**
 * Explicit domain → DTO mappers (Pure Fabrication) — the only place domain
 * objects are translated to the wire format; domain objects are never serialized
 * directly (docs/architecture.md §3). Empty at US007 scaffolding stage by design.
 */
package com.nortadas.web.mapper;
